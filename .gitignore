# OS / editors
.DS_Store
.idea/
.vscode/

# Go
bin/
coverage.out

# Python / MLX helper environments
.venv/
.venv-*/
__pycache__/
*.pyc

# Runtime / local state
#
# Anchored to the repo root (leading /) so these only match the top-level
# runtime-state directories a running daemon creates, not the source
# packages internal/artifacts and internal/worktrees (M2).
.devcadience/
/artifacts/
/worktrees/
*.db
*.db-shm
*.db-wal

# Secrets
.env
.env.*
!.env.example

# Logs
*.log
